#include <stdio.h>
#include <string.h>

typedef char user[25]; // nicknames for char variables named user with 25 bytes

int main() {

    // typedef = reserved keyword that gives an existing datatype a "nickname"

    user user1 = "Yui"; // you only need to call the "user" make a variable name and value

    return 0;
}
